version: 2.1
jobs:
 build:
   machine: true
   steps:
#      - checkout
     # start proprietary DB using private Docker image
     # with credentials stored in the UI
     - run: |
         docker run --rm -t -v $(pwd):/home coinbase/salus
